---
interface Props {
  id?: string;
  className?: string;
}

const { id, className = "" } = Astro.props;
---

<section
  id={id}
  class={`mx-auto w-full max-w-7xl space-y-4 px-4 py-4 sm:py-5 md:space-y-6 md:py-6 lg:py-8 xl:px-6 ${className}`}
>
  <slot />
</section>
