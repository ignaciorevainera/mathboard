# Mathboard

## Smoke test del theme toggle

1. Instalar dependencias del proyecto:
   npm install
2. Instalar navegador para Playwright (primera vez):
   npx playwright install chromium
3. Ejecutar smoke test del toggle:
   npm run test:smoke:theme-toggle
